// get token from browser if it exists
// if it exist verify token
// if expired logout
// if valid let inside
// if no token redirect to login page
export default async function (to) {
  console.log('MIDDLEWARE', to)
}
